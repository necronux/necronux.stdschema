# ==-----------------------------------------------------------== #
# SPDX-FileCopyrightText: © 2025 Nayan Patil <nayantsg@proton.me>
#
# SPDX-License-Identifier: Apache-2.0
# ==-----------------------------------------------------------== #

# Do not modify!
# This file was generated from a template using necronux.internal.pkl project.
# Any changes should be made to the template, not this file.

name: Policy-Branch-Protection
'on':
  pull_request: {}
permissions:
  contents: read
jobs:
  protect-develop-branch:
    name: Protect Develop Branch
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
    - name: Ensure PRs to develop branch are only from allowed branches
      shell: bash
      run: tools/scripts/ci/protect-develop-branch.sh
  protect-release-branches:
    name: Protect Release Branches
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
    - name: Ensure PRs to release branches are only from allowed branches
      shell: bash
      run: tools/scripts/ci/protect-release-branches.sh
  protect-stable-branches:
    name: Protect Stable Branches
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
    - name: Ensure PRs to stable branches are only from allowed branches
      shell: bash
      run: tools/scripts/ci/protect-stable-branches.sh
